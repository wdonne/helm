apiVersion: v1
kind: Service
metadata:
  name: sse
  namespace: {{ include "prefix" . }}sse
spec:
  selector:
    app.kubernetes.io/name: sse
  type: NodePort
  ports:
    - name: http
      port: 9000
      nodePort: 30021
      targetPort: http
      protocol: TCP
