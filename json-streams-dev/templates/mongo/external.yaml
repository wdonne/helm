apiVersion: v1
kind: Service
metadata:
  name: external
  namespace: {{ include "prefix" . }}mongo
spec:
  selector:
    app.kubernetes.io/name: mongo
  type: NodePort
  ports:
    - port: 27017
      name: db
      nodePort: 30017
      protocol: TCP
