apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: {{ include "prefix" . }}mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.mongo.storage }}
